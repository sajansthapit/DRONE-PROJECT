server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/drone_db
    username: postgres
    password: root
    initialization-mode: always
  rabbitmq:
    port: 5672
    username: guest
    password: guest

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://service-discovery/eureka/